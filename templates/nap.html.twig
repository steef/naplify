{% extends 'base.html.twig' %}

{% block title %}💤 Naplify{% endblock %}

{% block body %}

    <style>
        body {
            background: #121212;
            color: #E0E0E0;
            font: 18px/1.5 sans-serif;
            margin: 0;
        }
        .wrapper {
            margin: 1em auto;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        #napLength {
            font-size: 2em;
        }
    </style>

    <div class="wrapper">

        <h1>Enjoy 😴</h1>
        <div id="napLength" data-nap-length="{{ napLength }}">{{ napLength }}:00</div>

        <script>
            document.addEventListener('DOMContentLoaded', () => {
                startCountdown();
            });

            startCountdown();
        </script>
    </div>

{% endblock %}
